(()=>{const en=document.documentElement.lang==='en';for(const tile of document.querySelectorAll('.tile-repeat')){const value=tile.style.aspectRatio.split('/').map(Number);tile.dataset.ratio=String(value.length===2?value[0]/value[1]:value[0]||1);}function repeat(rows,cols){document.querySelectorAll('.tile-repeat').forEach(tile=>{tile.style.aspectRatio=String(Number(tile.dataset.ratio)*cols/rows);tile.style.backgroundSize=`${100/cols}% ${100/rows}%`;tile.dataset.rows=rows;tile.dataset.columns=cols;tile.setAttribute('aria-label',(tile.getAttribute('aria-label')||'').replace(/ — .*/,'')+` — ${rows} × ${cols}`);});document.querySelectorAll('[data-repeat]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.repeat===`${rows},${cols}`)));document.querySelectorAll('.repeat-status').forEach(s=>s.textContent=`${rows} × ${cols} · `+(en?'original tile proportions':'původní poměr stran dlaždice'));document.querySelectorAll('.tile-pair .tile-label').forEach(s=>{if(s.parentElement.querySelector('.tile-repeat'))s.textContent=(en?'Repeat review':'Kontrola opakování')+` · ${rows} × ${cols}`;});}document.querySelectorAll('[data-repeat]').forEach(b=>b.addEventListener('click',()=>repeat(...b.dataset.repeat.split(',').map(Number))));if(document.querySelector('.repeat-controls'))repeat(3,3);})();
